import React from 'react'
import './Chart.css';

const Chart = (props) => {
    const classes = 'card' + props.className;
    
    return(
        <div className = {classes} >


        </div>
    )
}

export default Chart;